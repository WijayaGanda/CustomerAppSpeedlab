import 'package:flutter/material.dart';

class ServiceCatalogWidget extends StatelessWidget {
  const ServiceCatalogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}